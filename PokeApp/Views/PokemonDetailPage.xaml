<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="PokeApp.PokemonDetailPage">
    <ContentPage.Content>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition Height="20" />
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackLayout Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Orientation="Horizontal"
                HorizontalOptions="CenterAndExpand">
                <Label Text="{Binding Pokemon.IdDisplay}" />
                <Label Text="{Binding Pokemon.Name}" />
            </StackLayout>
            <StackLayout Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Orientation="Horizontal"
                HorizontalOptions="CenterAndExpand">
                <Label Text="{Binding Pokemon.QueriedAtDisplay}" />
            </StackLayout>
            <StackLayout Grid.Row="2"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Orientation="Horizontal"
                HorizontalOptions="CenterAndExpand">
                <Image Source="{Binding Pokemon.SpriteFront}"
                    HorizontalOptions="StartAndExpand"
                    WidthRequest="100" />
                <Image Source="{Binding Pokemon.SpriteBack}"
                    HorizontalOptions="EndAndExpand"
                    WidthRequest="100" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>