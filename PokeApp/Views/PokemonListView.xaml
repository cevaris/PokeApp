<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:design="clr-namespace:PokeApp"
    BindingContext="{x:Static design:PokemonResourceListViewModel.Preview}"
    x:Class="PokeApp.PokemonListView">
    <ListView x:Name="PokemonList"
        ItemsSource="{Binding PokemonList}"
        ItemSelected="OnSelectedItem"
        RowHeight="50"
        ItemAppearing="Handle_ItemAppearing">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ImageCell Text="{Binding Id}"
                    Detail="{Binding Identifier}"
                    ImageSource="{Binding FrontSpriteUrl}" />
            </DataTemplate>
        </ListView.ItemTemplate>
        <ListView.Footer>
            <ActivityIndicator IsRunning="{Binding IsLoading}"
                IsVisible="{Binding IsLoading}" />
        </ListView.Footer>
    </ListView>
</StackLayout>